spring:
  cloud:
    config:
      discovery:
        serviceId: "configserver"
      fail-fast: true
  application:
    name: zuul-service

eureka:
  client:
    registerWithEureka: false
    fetchRegistry: true
    service-url:
      defaultZone: http://service-discovery:8761/eureka

