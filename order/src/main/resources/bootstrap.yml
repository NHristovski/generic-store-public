spring:
  cloud:
    config:
      discovery:
        serviceId: "configserver"
      fail-fast: true
  application:
    name: order-service

eureka:
  client:
    service-url:
      defaultZone: http://service-discovery:8761/eureka
