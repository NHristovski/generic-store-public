spring:
  cloud:
    config:
      discovery:
        serviceId: "configserver"
      fail-fast: true
  application:
    name: shipping-service

eureka:
  client:
    service-url:
      defaultZone: http://service-discovery:8761/eureka
