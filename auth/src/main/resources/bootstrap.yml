spring:
  cloud:
    config:
      discovery:
        serviceId: "configserver"
        enabled: true
      fail-fast: true
  application:
    name: auth-service

eureka:
  client:
    service-url:
      defaultZone: http://service-discovery-priv-service/eureka
