spring:
  cloud:
    config:
      discovery:
        serviceId: "configserver"
        enabled: true
      fail-fast: true
  application:
    name: payment-service

eureka:
  client:
    service-url:
      defaultZone: http://service-discovery:8761/eureka
