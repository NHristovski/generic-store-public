spring:
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: "configserver"
      fail-fast: true
  application:
    name: user-service

eureka:
  client:
    service-url:
      defaultZone: http://service-discovery:8761/eureka
