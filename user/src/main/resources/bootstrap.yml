spring:
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: "configserver"
      fail-fast: true
  application:
    name: user-service

eureka:
  client:
    service-url:
      defaultZone: http://service-discovery-priv-service/eureka
