spring:
  cloud:
    config:
      uri: http://config-server-service
      fail-fast: true
  application:
    name: user-service

eureka:
  client:
    service-url:
      defaultZone: http://service-discovery-priv-service/eureka
